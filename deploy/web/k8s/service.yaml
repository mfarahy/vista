apiVersion: v1
kind: Service
metadata:
  name: vista-web
  labels:
    app: vista-web
spec:
  selector:
    app: vista-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
