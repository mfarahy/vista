apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vista-backend
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: api.vista.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vista-backend
                port:
                  number: 80
