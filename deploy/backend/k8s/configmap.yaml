apiVersion: v1
kind: ConfigMap
metadata:
  name: vista-backend-config
  labels:
    app: vista-backend
data:
  CORS_ORIGIN: "https://vista.example.com"
  OPENAI_BASE_URL: "https://api.openai.com/v1"
  OPENAI_MODEL: "gpt-4o-mini"
