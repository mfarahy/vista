replicaCount: 3
image:
  repository: ghcr.io/example/vista-web
  tag: "1.0.0"
config:
  NEXT_PUBLIC_API_BASE_URL: "https://api.vista.example.com"
  NEXT_PUBLIC_APP_URL: "https://vista.example.com"

ingress:
  enabled: true
  host: vista.example.com
  tls:
    - secretName: vista-web-tls
