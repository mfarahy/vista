import type { ExposeContent, Property, PropertyImage } from "./types";
import { FEATURE_OPTIONS, PROPERTY_TYPES } from "./types";

const esc = (value: unknown) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const propertyLabel = (key: string) => PROPERTY_TYPES.find(([value]) => value === key)?.[1] || key;
const featureLabel = (key: string) => FEATURE_OPTIONS.find(([value]) => value === key)?.[1] || key;
const money = (value?: number | null) => value ? new Intl.NumberFormat("de-DE", { style: "currency", currency: "EUR", maximumFractionDigits: 0 }).format(value) : "Auf Anfrage";
const imageSrc = (image: PropertyImage) => image.url;

function gallery(images: PropertyImage[]) {
  const sorted = [...images].sort((a, b) => a.sequence - b.sequence);
  if (!sorted.length) return `<div class="gallery-empty">Bilder werden nach dem Upload hier angezeigt.</div>`;
  const countClass = `gallery-${Math.min(sorted.length, 6)}`;
  return `<div class="gallery ${countClass}">${sorted.map((image, index) => `<figure class="gallery-item item-${index + 1}"><img src="${esc(imageSrc(image))}" alt="${esc(image.fileName)}" /></figure>`).join("")}</div>`;
}

export function exposeHTML(property: Property, content: ExposeContent) {
  const images = [...property.images].sort((a, b) => a.sequence - b.sequence);
  const cover = images.find((image) => image.isCover) || images[0];
  const facts = content.factualSnapshot.length ? content.factualSnapshot : [property.livingArea && `${property.livingArea} m² Wohnfläche`, property.rooms && `${property.rooms} Zimmer`, property.bedrooms && `${property.bedrooms} Schlafzimmer`, property.bathrooms && `${property.bathrooms} Badezimmer`].filter(Boolean) as string[];
  const features = [...property.selectedFeatures, ...(property.additionalFeatures ? [property.additionalFeatures] : [])];
  return `<!doctype html><html lang="de"><head><meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" /><title>${esc(content.title)}</title><style>${printCSS()}</style></head><body>
  <main class="expose">
    <section class="cover page"><div class="cover-image" style="${cover ? `background-image:url('${esc(imageSrc(cover))}')` : ""}"><div class="cover-fallback">RAUMWERK</div></div><div class="cover-copy"><div class="eyebrow">IMMOBILIEN-EXPOSÉ · ${esc(property.transactionType === "sale" ? "ZUM KAUF" : "ZUR MIETE")}</div><h1>${esc(content.title)}</h1><p class="cover-location">${esc(property.city || "")}${property.district ? ` · ${esc(property.district)}` : ""}</p><div class="cover-price">${esc(money(property.transactionType === "sale" ? property.askingPrice : property.coldRent))}<small>${property.transactionType === "sale" ? "Kaufpreis" : "Kaltmiete"}</small></div></div></section>
    <section class="page overview"><div class="section-kicker">01 · AUF EINEN BLICK</div><div class="overview-head"><div><h2>Ein Zuhause mit<br/><em>Persönlichkeit.</em></h2></div><p>${esc(content.shortDescription)}</p></div><div class="facts">${facts.map((fact) => `<div class="fact"><strong>${esc(fact.split(" ")[0])}</strong><span>${esc(fact.split(" ").slice(1).join(" "))}</span></div>`).join("")}</div><div class="split"><div><div class="section-kicker">HIGHLIGHTS</div><ul class="highlights">${content.highlights.map((item) => `<li>${esc(item)}</li>`).join("")}</ul></div><div class="intro-copy"><p>${esc(content.shortDescription)}</p></div></div></section>
    <section class="page text-page"><div class="section-kicker">02 · BESCHREIBUNG</div><h2>Räume, die<br/><em>Geschichten erzählen.</em></h2><div class="body-copy">${content.mainDescription.split("\n").map((paragraph) => `<p>${esc(paragraph)}</p>`).join("")}</div>${property.roomsData.length ? `<div class="rooms"><div class="section-kicker">RAUMPROGRAMM</div>${content.roomDescriptions.map((room) => `<div class="room"><div><strong>${esc(room.name)}</strong><span>${esc(property.roomsData.find((item) => item.id === room.roomId)?.size ? `${property.roomsData.find((item) => item.id === room.roomId)?.size} m²` : "Raum")}</span></div><p>${esc(room.description)}</p></div>`).join("")}</div>` : ""}</section>
    <section class="page location-page"><div class="section-kicker">03 · LAGE &amp; UMFELD</div><div class="location-grid"><div><h2>Die Lage.<br/><em>Ihr neuer Kontext.</em></h2><div class="body-copy">${content.locationDescription.split("\n").map((paragraph) => `<p>${esc(paragraph)}</p>`).join("")}</div></div><div class="location-card"><div class="map-placeholder"><span>LOC</span><b>${esc(property.city || "Standort")}</b></div>${Object.entries(property.surroundings || {}).filter(([, value]) => value).map(([key, value]) => `<div class="location-line"><span>${esc(key)}</span><b>${esc(value)}</b></div>`).join("")}</div></div></section>
    <section class="page photo-page"><div class="section-kicker">04 · IMPRESSIONEN</div><h2>Einblicke in<br/><em>Ihr neues Zuhause.</em></h2>${gallery(images)}</section>
    <section class="closing"><div class="closing-mark">R</div><div><div class="eyebrow">RAUMWERK · EXPOSÉ</div><h2>Bereit für den<br/><em>nächsten Schritt?</em></h2><p>${esc(content.targetAudience)}</p></div><div class="closing-foot">Fakten und Angaben basieren auf den bereitgestellten Informationen.</div></section>
  </main></body></html>`;
}

export function printCSS() { return `@page{size:A4;margin:0}*{box-sizing:border-box}body{margin:0;background:#eee9e2;color:#202522;font-family:Arial,Helvetica,sans-serif}.expose{max-width:210mm;margin:auto;background:#f7f4ef}.page{min-height:297mm;padding:20mm 18mm;break-after:page;position:relative;overflow:hidden}.cover{padding:0;display:flex;flex-direction:column;background:#202522;color:#f7f4ef}.cover-image{height:174mm;background:#6f7c73 center/cover no-repeat;position:relative}.cover-fallback{height:100%;display:grid;place-items:center;color:#b7c4b8;font-size:32px;letter-spacing:8px}.cover-copy{padding:12mm 18mm 16mm;position:relative;flex:1}.eyebrow,.section-kicker{font-size:9px;letter-spacing:2px;font-weight:700;color:#77887b}.cover .eyebrow{color:#b7c4b8}.cover h1{font-family:Georgia,serif;font-weight:400;font-size:36px;line-height:1.05;max-width:150mm;margin:14mm 0 5mm}.cover-location{font-size:14px;color:#d1dbd1}.cover-price{position:absolute;right:18mm;bottom:17mm;text-align:right;font-size:22px}.cover-price small{display:block;font-size:9px;color:#b7c4b8;letter-spacing:1px;margin-top:4px}.overview-head{display:grid;grid-template-columns:1.1fr 1fr;gap:18mm;align-items:end;margin:15mm 0}.overview h2,.text-page h2,.location-page h2,.photo-page h2,.closing h2{font:400 34px/1.05 Georgia,serif;margin:0}.overview h2 em,.text-page h2 em,.location-page h2 em,.photo-page h2 em,.closing h2 em{color:#819485}.overview-head p,.intro-copy p{font-size:15px;line-height:1.65;color:#606b64}.facts{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid #d7d5cd;border-bottom:1px solid #d7d5cd;padding:7mm 0;margin-bottom:18mm}.fact strong{display:block;font:400 27px Georgia,serif}.fact span{font-size:9px;display:block;color:#778078;margin-top:4px;text-transform:uppercase;letter-spacing:1px}.split{display:grid;grid-template-columns:1fr 1fr;gap:20mm}.highlights{padding:0;margin:8mm 0;list-style:none}.highlights li{padding:4mm 0;border-bottom:1px solid #dedbd3;font-size:13px}.highlights li:before{content:'+';color:#819485;margin-right:10px;font-weight:bold}.text-page{background:#ece9e2}.text-page h2,.location-page h2,.photo-page h2{margin:12mm 0 14mm}.body-copy{max-width:145mm;font:16px/1.7 Georgia,serif;color:#4b554f}.body-copy p{margin:0 0 7mm;white-space:pre-line}.rooms{margin-top:15mm}.room{display:grid;grid-template-columns:42mm 1fr;gap:10mm;padding:5mm 0;border-top:1px solid #d1d2c9}.room strong,.room span{display:block}.room strong{font-size:13px}.room span{font-size:10px;color:#778078;margin-top:3px}.room p{margin:0;font-size:12px;line-height:1.55;color:#5f6861}.location-page{background:#f7f4ef}.location-grid{display:grid;grid-template-columns:1fr .8fr;gap:15mm}.location-card{background:#202522;color:#f2f3ed;padding:7mm;align-self:start}.map-placeholder{height:65mm;background:linear-gradient(135deg,#52655a,#809388);display:flex;flex-direction:column;justify-content:end;padding:5mm;margin-bottom:6mm}.map-placeholder span{font-size:36px;color:#c4d0c4;font-family:Georgia,serif}.map-placeholder b{font-size:13px}.location-line{padding:4mm 0;border-bottom:1px solid #526058;font-size:10px}.location-line span{display:block;color:#aeb9ae;text-transform:uppercase;letter-spacing:1px;margin-bottom:2px}.location-line b{font-size:12px;font-weight:400}.photo-page{background:#202522;color:#f7f4ef}.photo-page .section-kicker{color:#a9b9aa}.photo-page h2 em{color:#afc0af}.gallery{display:grid;gap:4mm;height:160mm}.gallery-item{margin:0;overflow:hidden;background:#65736b}.gallery-item img{width:100%;height:100%;object-fit:cover;display:block}.gallery-1{grid-template-columns:1fr}.gallery-2,.gallery-4,.gallery-6{grid-template-columns:repeat(2,1fr)}.gallery-3,.gallery-5{grid-template-columns:1.35fr 1fr;grid-template-rows:repeat(2,1fr)}.gallery-3 .item-1,.gallery-5 .item-1{grid-row:span 2}.gallery-5 .item-4{grid-column:2}.gallery-5 .item-5{grid-column:2}.gallery-6{grid-template-rows:repeat(3,1fr)}.gallery-4{grid-template-rows:repeat(2,1fr)}.gallery-2{grid-template-rows:1fr}.gallery-empty{height:80mm;display:grid;place-items:center;border:1px dashed #77887b;color:#a9b9aa}.closing{min-height:130mm;padding:24mm 18mm;background:#819485;color:#f7f4ef;position:relative}.closing .eyebrow{color:#e0e7df}.closing h2{margin:15mm 0 8mm}.closing h2 em{color:#e0e7df}.closing p{max-width:100mm;line-height:1.6;color:#e4ebe3}.closing-mark{position:absolute;right:18mm;top:18mm;font:100px Georgia,serif;color:#b5c5b6}.closing-foot{position:absolute;bottom:12mm;font-size:9px;color:#dbe5db}@media print{body{background:#f7f4ef}.expose{width:210mm}.page{min-height:297mm}.closing{break-before:page}}`; }
