# VLM Grounding Experiment — Annotated Recognition Image

**Fixture:** `c658e915-9247-4904-8032-717dd11ecfdd.jpg` + `recognition-c658e915-9247-4904-8032-717dd11ecfdd.json`
**Model:** `gpt-5.6-luna` (OpenAI, structured outputs via `zodResponseFormat`)
**Branch:** `feat/vlm-annotated-grounding`
**Debug page:** `/debug/floorplan-recognition`

## Question

*Does an annotated raw-recognition image (labels `wall-*`, `door-*`, `window-*`, … rendered
over the original image) significantly improve VLM object-ID grounding?*

## Method

- **BEFORE (baseline):** `before-single-image.json` — POST to
  `/api/debug/floorplan-recognition/vlm-analysis` with **only** the original image + raw JSON
  (the previous POC behaviour; the endpoint still supports this path).
- **AFTER (annotated):** `after-annotated-run1.json` / `after-annotated-run2.json` — the same
  endpoint driven through the real `/debug/floorplan-recognition` page, so the annotated image was
  generated by the actual frontend renderer (`frontend/lib/annotated-recognition-image.ts`) and
  sent as a second image alongside original + raw JSON. Two runs capture VLM variance.
- Ground truth for "correct ID" was derived from the raw geometry itself
  (`raw-geometry-summary.txt`: per-object bounding boxes in image pixels).

## Annotated image verification (before calling the VLM)

`annotated-recognition.png` (1500×1060, full source resolution, same coordinate system):

- every raw object bbox shows strong overlay presence (18–76 % changed pixels, see
  `annotated-pixel-probe.json`) → overlays/labels are drawn at the correct raw coordinates;
- global change rate is only **7.77 %** → the annotated image stays visually close to the
  source (semi-transparent overlays, architecture still readable);
- category-specific colors + always-on ID labels (`wall-0…4`, `door-0…5`, `window-0…4`,
  `entry_door-0`, `kitchen-0`) are rendered from the RAW JSON only (no normalize/rooms/3D code).

## Results

### East facade
|                                                          | BEFORE  | AFTER                                               |
| -------------------------------------------------------- | ------- | --------------------------------------------------- |
| Correct IDs (`wall-3` + `wall-1` = same_continuous_wall) | ✅ 0.88  | ✅ 0.94 (run 2), 0.82 (run 1)                        |
| Reasoning quality                                        | generic | explicitly names window-2 as the separating opening |

### window-2 host walls
|     | BEFORE                   | AFTER                           |
| --- | ------------------------ | ------------------------------- |
| IDs | wall-3 + wall-1 ✅ (0.95) | wall-3 + wall-1 ✅ (0.96 / 0.84) |

### window-3 (kitchen window) — the clearest grounding win
Geometric ground truth: window-3 spans x309–429; `wall-3` starts at x=433, so its only possible
host is **wall-4** (the west/top-left wall system).

|             | host claimed      | confidence                                                                                                  | verdict |
| ----------- | ----------------- | ----------------------------------------------------------------------------------------------------------- | ------- |
| BEFORE      | `wall-3` 0.94     | ❌ geometrically impossible (wall-3 bbox starts at x=433, window ends x=429)                                 |
| AFTER run 1 | `wall-3` 0.93     | ❌ same error                                                                                                |
| AFTER run 2 | **`wall-4` 0.76** | ✅ correct, with explicit reasoning ("north exterior wall, whose left-side segment is identified as wall-4") |

### Kitchen boundary walls
|       | BEFORE                   | AFTER (run 2)                                         |
| ----- | ------------------------ | ----------------------------------------------------- |
| walls | wall-3 + wall-4 + wall-2 | **wall-4 + wall-2** (dropped the impossible wall-3) ✅ |

### Hallway
|               | BEFORE                        | AFTER (run 2)                                                                    |
| ------------- | ----------------------------- | -------------------------------------------------------------------------------- |
| boundaryWalls | wall-4, wall-2, wall-1 (0.72) | wall-2, wall-4, wall-0, wall-1 (0.78) — also grounds the south `wall-0` fragment |

### Terrace vs living
|           | BEFORE                                                                                              | AFTER                                                                             |
| --------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| terrace-0 | wall-1 only; listed window-2/window-4 as terrace *openings* (wrong — those are living-room windows) | boundary = east facade `wall-3 + wall-1`; run 1 correctly emits **no openings** ✅ |

### False positives
|                                  | BEFORE      | AFTER                                                                             |
| -------------------------------- | ----------- | --------------------------------------------------------------------------------- |
| window-1 → likely_false_positive | ✅ 0.98      | ✅ 0.97 / 0.98 (all runs)                                                          |
| suspicious walls                 | wall-0 0.65 | run 2 adds wall-4 (complex multi-segment) + window-4 (terrace door) as suspicious |

### Uncertain behaviour (new)
- `window-1` host: `uncertain`, confidence **0.08–0.18** instead of guessing.
- run 2 emitted an explicit second, low-confidence entry for door-0 (0.30, "exact host segment
  ambiguous") instead of a single overconfident guess.
- run 1 connection `wall-0 + wall-1 → uncertain 0.42` where the entrance/windfang gap is
  architecturally unclear.

### ID grounding accuracy per opening (checked against raw bboxes)
| opening                | BEFORE               | AFTER run 1                    | AFTER run 2                                             |
| ---------------------- | -------------------- | ------------------------------ | ------------------------------------------------------- |
| window-2 → east facade | wall-3+wall-1 ✅      | wall-3+wall-1 ✅                | wall-3+wall-1 ✅                                         |
| window-3 (kitchen)     | wall-3 ❌ (0.94)      | wall-3 ❌ (0.93)                | **wall-4 ✅ (0.76)**                                     |
| door-0                 | wall-4 ✅ (0.78)      | wall-0 ❌ (0.90, overconfident) | wall-0+wall-4 ✅ + uncertain 0.30                        |
| door-5                 | wall-2 only          | wall-2 only                    | wall-3+wall-2 ✅ (both segment portions, matches bboxes) |
| entry_door-0           | wall-1+wall-0 ✅ 0.61 | wall-0+wall-1 ✅ 0.68           | wall-0+wall-1 ✅ 0.91                                    |

Confidence calibration also improved with the annotated image: ambiguous groundings drop to
0.42–0.76 instead of asserting 0.9+, and `uncertain` relationships appear where identification
is unreliable.

## Verdict

**Yes — the annotated recognition image materially improves object-ID grounding.**
- Architectural understanding was already good and stays the same (rooms, continuity,
  false-positive detection).
- ID grounding improves measurably: the kitchen window host (window-3 → wall-4, previously wrong
  with 0.94 confidence), kitchen boundary (impossible wall-3 dropped), door-5 dual-segment hosts,
  terrace-vs-living distinction, and entry-door grounding all became correct or cleaner.
- Residual risk: grounding is still not perfect (run 1 still mis-grounded window-3), so the
  `uncertain` + confidence rules remain essential; run-to-run variance exists and the schema
  now makes that variance visible instead of hiding it behind high confidence.

## Artifacts

| file                                                      | content                                                                |
| --------------------------------------------------------- | ---------------------------------------------------------------------- |
| `before-single-image.json`                                | BEFORE run (original + JSON only), model `gpt-5.6-luna`, 33.5 s        |
| `after-annotated-run1.json` / `after-annotated-run2.json` | AFTER runs (original + annotated + JSON), 35.6 s / 25.2 s              |
| `annotated-recognition.png`                               | full-resolution annotated recognition image (frontend renderer output) |
| `2-annotated-recognition-panel.png`                       | debug panel: Original vs Annotated side by side                        |
| `3-overlay-raw-plus-vlm.png`                              | RAW + VLM relationship overlay                                         |
| `4-vlm-only-interpretation.png`                           | VLM-only interpretation overlay                                        |
| `5-vlm-grounding.png`                                     | VLM Grounding section                                                  |
| `6-vlm-findings.png`                                      | VLM Findings panel                                                     |
| `7-full-page.png`                                         | full debug page after analysis                                         |
| `annotated-pixel-probe.json`                              | per-object overlay verification + global change rate                   |
| `raw-geometry-summary.txt`                                | ground-truth bbox summary of the raw recognition JSON                  |

---

# Topology Contract Experiment (`feat/vlm-topology-contract`)

**Branch:** `feat/vlm-topology-contract` · **Model:** `gpt-5.6-luna` (via the deployed
expose-service's `OPENAI_API_KEY`, mapped from the CI/CD pipeline variables)

Refines the VLM output into a clean **architectural topology / cleanup graph**:

- The VLM's only responsibility is understanding relationships between RAW objects and
  flagging incorrect detections. It never emits coordinates or geometry.
- Output = four categories (`wallRelationships`, `openings`, `objectClassifications`,
  `rooms`) + a compact `topologySummary`.
- Every ID is validated against the RAW JSON (`validateVlmAnalysis`); invalid IDs are
  filtered and surfaced as warnings. All runs below reported **0 warnings**.

## Results (canonical run — `after-topology-contract.json`)

- **East facade continuity:** `wall-3 + wall-1` → `same_continuous_wall` (0.96); the
  topology summary lists the same pair under `continuousWalls`. ✅
- **window-2 → east facade:** host `wall-1`, `interrupts_wall` (0.97), "vertical window
  embedded in the eastern exterior facade". ✅
- **window-3 (kitchen window):** this run grounded it to `wall-3` (0.95) — a known weak
  spot (bbox starts at x=433, window ends x=429); prior runs sometimes correctly chose
  `wall-4`. Confirms run-to-run variance on ambiguous openings.
- **Kitchen:** `kitchen-0` boundary `wall-4 + wall-2 + wall-3`, openings
  `window-3, door-4, door-5` (0.93). (Including wall-3 is defensible here but the prior
  grounding run dropped it — see above.)
- **Hallway / circulation:** `hallway-0` (Diele) `wall-4 + wall-2 + wall-1` with
  `door-2, door-3, door-4`; `entrance-0` (Windfang) `wall-0 + wall-1 + wall-4`.
- **False positive:** `window-1` → `likely_false_positive` (0.98–0.99); its opening
  association drops to `uncertain` 0.12 instead of guessing a host. ✅
- **Stair contamination:** `wall-4` → `suspicious` (0.72–0.88), "geometry should not be
  trusted as one clean wall"; `window-4` → `suspicious` in run 2/3. ✅
- **New relationship types work:** `perpendicular`, `extension_of` were used correctly.
- **ID grounding integrity:** every ID in all runs existed in the RAW JSON (0 warnings).
- **Uncertainty discipline:** ambiguous hosts (window-1) are `uncertain` with low
  confidence; the model does not force relationships.

## Prompt refinement from the runs

An initial run classified center-line objects (`door_center_line-*`, …) and under-listed
rooms. The prompt now explicitly forbids classifying center-line detections and requires
enumerating **all** major rooms. After the fix: exactly the 18 architectural objects are
classified and 6 rooms are emitted.

## Artifacts (topology contract)

| file                                                         | content                                                                |
| ------------------------------------------------------------ | ---------------------------------------------------------------------- |
| `after-topology-contract.json`                               | canonical run (original + annotated + RAW JSON), 0 warnings, ~36 s     |
| `after-topology-contract-run1.json`                          | first run before the prompt refinement (6 rooms, no center-line noise) |
| `annotated-topology-contract.png`                            | annotated recognition image rendered by `scripts/vlm-topology-experiment.ts` |
| `8-ui-full-page-topology.png`                                | full debug page after topology analysis                                 |
| `9-topology-section.png`                                     | **Architectural Topology** section (continuous walls / corners / T-junctions / false positives / suspicious) |
| `10-overlay-topology.png`                                    | main overlay: RAW + VLM topology relationships                          |
| `11-highlight-continuous-wall.png`                           | overlay after clicking `wall-3 + wall-1` (highlights the RAW polygons)  |
| `12-highlight-false-positive.png`                            | overlay after clicking `window-1` (highlight + FP marker)               |
| `13-highlight-suspicious.png`                                | overlay after clicking `wall-4` (suspicious highlight)                  |
| `14-topology-only-mode.png`                                  | "Topology only" mode (reconstruction hidden)                            |

## Reproduce (CLI)

```bash
# from expose-service/, with OPENAI_API_KEY set (e.g. from the pipeline variable
# mapped to the deployed expose-service env)
OPENAI_API_KEY=... OPENAI_MODEL=gpt-5.6-luna npx tsx scripts/vlm-topology-experiment.ts
```

## Reproduce (UI)

1. Start the frontend (`:3000`) and expose-service (`:4000`, `OPENAI_API_KEY` set).
2. Open `/debug/floorplan-recognition`, click **Load fixture**.
3. Inspect the **Annotated Recognition Image** panel (Original vs Annotated) before invoking the VLM.
4. Click **Analyze with VLM** — the page posts original image + annotated PNG + raw JSON to
   `/api/debug/floorplan-recognition/vlm-analysis` and renders findings, grounding list, and overlay.